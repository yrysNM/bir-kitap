import { Page } from "../layouts/Page"
import { Header } from "../components/Header"

export const ForgotPassword = () => {
    return (
        <Page>
            <Header isCustomHeader={false} title={"Forgot Password"} />
        </Page>
    )
}
